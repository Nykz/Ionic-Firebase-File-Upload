import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'file-transfer-firebase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
